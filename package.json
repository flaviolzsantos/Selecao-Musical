{
  "name": "companyhero",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "vitest --run",
    "test:coverage": "npx vitest run --coverage",
    "dockerBuild": "docker build -t music-temperature-api .",
    "dockerRun": "docker run -p 3000:3000 --env-file .env music-temperature-api"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "dotenv": "^16.4.5",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "c8": "^10.1.2",
    "supertest": "^7.0.0",
    "vitest": "^0.21.0"
  }
}
